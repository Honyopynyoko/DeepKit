"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findParentPath = void 0;
const fs_1 = require("fs");
const path_1 = require("path");
function findParentPath(path, origin = __dirname) {
    let current = origin;
    while (!fs_1.existsSync(path_1.join(current, path))) {
        const nextFolder = path_1.resolve(current, '..');
        if (nextFolder === current) {
            return undefined;
        }
        current = nextFolder;
    }
    return path_1.join(current, path);
}
exports.findParentPath = findParentPath;
//# sourceMappingURL=utils.js.map