"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
Object.defineProperty(exports, "__esModule", { value: true });
const globals_1 = require("@jest/globals");
require("reflect-metadata");
const type_1 = require("@deepkit/type");
const injector_1 = require("@deepkit/injector");
const service_container_1 = require("../src/service-container");
const module_1 = require("../src/module");
const myModuleConfig = new module_1.AppModuleConfig({
    param1: type_1.t.string.minLength(5),
    param2: type_1.t.number.minimum(100)
});
class MyModuleConfigFull extends myModuleConfig.all() {
}
let ModuleService = class ModuleService {
    constructor(config) {
        this.config = config;
    }
};
ModuleService = __decorate([
    injector_1.injectable(),
    __metadata("design:paramtypes", [MyModuleConfigFull])
], ModuleService);
const MyModule = new module_1.AppModule({
    config: myModuleConfig,
    providers: [
        ModuleService
    ],
    exports: [ModuleService]
}, 'myModule');
const appModuleConfig = new module_1.AppModuleConfig({
    database: type_1.t.string.default('mongodb://localhost/my-app'),
    debug: type_1.t.boolean.default(false),
});
class MyServiceConfig extends appModuleConfig.slice(['debug']) {
}
const debugConfigToken = appModuleConfig.token('debug');
globals_1.expect(Object.getPrototypeOf(Object.getPrototypeOf(MyServiceConfig)) === injector_1.ConfigSlice).toBe(true);
let MyService = class MyService {
    constructor(config) {
        this.config = config;
    }
    isDebug() {
        return this.config.debug;
    }
};
MyService = __decorate([
    injector_1.injectable(),
    __metadata("design:paramtypes", [MyServiceConfig])
], MyService);
let MyService2 = class MyService2 {
    constructor(debug) {
        this.debug = debug;
    }
};
MyService2 = __decorate([
    injector_1.injectable(),
    __param(0, injector_1.inject(debugConfigToken)),
    __metadata("design:paramtypes", [Boolean])
], MyService2);
const myAppModule = new module_1.AppModule({
    providers: [
        MyService,
        MyService2,
    ],
    imports: [
        MyModule,
    ],
    config: appModuleConfig,
});
function getMyServiceFor(module, service) {
    const serviceContainer = new service_container_1.ServiceContainer(module);
    return serviceContainer.getRootInjectorContext().get(service);
}
globals_1.test('loadConfig', () => {
    // Application.create(AppModule).loadConfigFromEnvFile('.env').run();
    // Application.create(AppModule).loadConfigFromEnvVariables().run();
    // Application.create(AppModule).loadConfigFromEnvVariable('APP_CONFIG').run();
});
globals_1.test('import', () => {
    // {
    //     expect(() => getMyServiceFor(AppModule, ModuleService)).toThrow(
    //         'Configuration for module myModule is invalid. Make sure the module is correctly configured. Error: myModule.param1(required): Required value is undefined, myModule.param2(required): Required value is undefined'
    //     );
    // }
    {
        globals_1.expect(() => getMyServiceFor(myAppModule.configure({ myModule: { param1: '23' } }), ModuleService)).toThrow('Configuration for module myModule is invalid. Make sure the module is correctly configured. Error: myModule.param1(minLength): Min length is 5, myModule.param2(required): Required value is undefined');
    }
    {
        globals_1.expect(() => getMyServiceFor(myAppModule.configure({ myModule: { param1: '12345' } }), ModuleService)).toThrow('Configuration for module myModule is invalid. Make sure the module is correctly configured. Error: myModule.param2(required): Required value is undefined');
    }
    {
        globals_1.expect(() => getMyServiceFor(myAppModule.configure({ myModule: { param1: '12345', param2: 55 } }), ModuleService)).toThrow('Configuration for module myModule is invalid. Make sure the module is correctly configured. Error: myModule.param2(minimum): Number needs to be greater than or equal to 100');
    }
    {
        globals_1.expect(() => getMyServiceFor(myAppModule.configure({ myModule: { param1: '12345', param2: '55' } }), ModuleService)).toThrow('Configuration for module myModule is invalid. Make sure the module is correctly configured. Error: myModule.param2(minimum): Number needs to be greater than or equal to 100');
    }
    {
        const myService = getMyServiceFor(myAppModule.configure({ myModule: { param1: '12345', param2: 100 } }), ModuleService);
        globals_1.expect(myService.config).toEqual({ param1: '12345', param2: 100 });
    }
    {
        //@deepkit/type does automatically soft-type casting
        const myService = getMyServiceFor(myAppModule.configure({ myModule: { param1: '12345', param2: '100' } }), ModuleService);
        globals_1.expect(myService.config).toEqual({ param1: '12345', param2: 100 });
    }
});
globals_1.test('basic configured', () => {
    globals_1.expect(new MyService({ debug: true }).isDebug()).toBe(true);
    globals_1.expect(new MyService({ debug: false }).isDebug()).toBe(false);
    const myConfiguredApp = myAppModule.configure({ myModule: { param1: '12345', param2: '100' } });
    {
        const myService = getMyServiceFor(myConfiguredApp, MyService);
        globals_1.expect(myService.isDebug()).toBe(false);
    }
    {
        const myService = getMyServiceFor(myConfiguredApp.configure({
            debug: false
        }), MyService);
        globals_1.expect(myService.isDebug()).toBe(false);
    }
    {
        const myService = getMyServiceFor(myConfiguredApp.configure({
            debug: true
        }), MyService);
        globals_1.expect(myService.isDebug()).toBe(true);
    }
    {
        const myService2 = getMyServiceFor(myConfiguredApp.configure({
            debug: false
        }), MyService2);
        globals_1.expect(myService2.debug).toBe(false);
    }
    {
        const myService2 = getMyServiceFor(myConfiguredApp.configure({
            debug: true
        }), MyService2);
        globals_1.expect(myService2.debug).toBe(true);
    }
});
globals_1.test('config inheritance', () => {
    const appModule = new module_1.AppModule({
        providers: [
            MyService,
            MyService2,
        ],
        imports: [
            MyModule.configure({ param1: '12345' }),
        ],
        config: appModuleConfig,
    });
    globals_1.expect(() => appModule.getImports()[0].getConfig()).toThrow('Configuration for module myModule is invalid. Make sure the module is correctly configured. Error: myModule.param2(required): Required value is undefined');
    globals_1.expect(appModule.configure({ myModule: { param2: 555 } }).getImports()[0].getConfig()).toEqual({ param1: '12345', param2: 555 });
});
globals_1.test('config inheritance', () => {
    const appModule = new module_1.AppModule({
        providers: [
            MyService,
            MyService2,
        ],
        imports: [
            MyModule.configure({ param1: '12345' }),
        ],
        config: appModuleConfig,
    });
    globals_1.expect(() => appModule.getImports()[0].getConfig()).toThrow('Configuration for module myModule is invalid. Make sure the module is correctly configured. Error: myModule.param2(required): Required value is undefined');
    globals_1.expect(appModule.configure({ myModule: { param2: 555 } }).getImports()[0].getConfig()).toEqual({ param1: '12345', param2: 555 });
});
globals_1.test('configured provider', () => {
    class Transporter {
    }
    class Logger {
        constructor() {
            this.transporter = [];
        }
        addTransport(transport) {
            this.transporter.push(transport);
        }
    }
    const appModule = new module_1.AppModule({
        providers: [
            Transporter,
            Logger,
        ],
    });
    {
        const fork = appModule.clone();
        fork.setupProvider(Logger).addTransport('first').addTransport('second');
        globals_1.expect(appModule.getConfiguredProviderRegistry().get(Logger).length).toBe(0);
        globals_1.expect(fork.getConfiguredProviderRegistry().get(Logger).length).toBe(2);
        const clone = fork.clone();
        globals_1.expect(clone.getConfiguredProviderRegistry().get(Logger).length).toBe(2);
    }
    {
        const logger = new service_container_1.ServiceContainer(appModule.setup((module) => {
            module.setupProvider(Logger).addTransport('first').addTransport('second');
        })).getInjectorFor(appModule).get(Logger);
        globals_1.expect(logger.transporter).toEqual(['first', 'second']);
    }
    {
        const logger = new service_container_1.ServiceContainer(appModule.setup((module) => {
            module.setupProvider(Logger).transporter = ['first', 'second', 'third'];
        })).getInjectorFor(appModule).get(Logger);
        globals_1.expect(logger.transporter).toEqual(['first', 'second', 'third']);
    }
    {
        const logger = new service_container_1.ServiceContainer(appModule.setup((module) => {
            module.setupProvider(Logger).addTransport(new Transporter);
        })).getInjectorFor(appModule).get(Logger);
        globals_1.expect(logger.transporter[0] instanceof Transporter).toBe(true);
    }
    {
        const logger = new service_container_1.ServiceContainer(appModule.setup((module) => {
            module.setupProvider(Logger).addTransport(injector_1.injectorReference(Transporter));
        })).getInjectorFor(appModule).get(Logger);
        globals_1.expect(logger.transporter[0] instanceof Transporter).toBe(true);
    }
    {
        const logger = new service_container_1.ServiceContainer(appModule).getInjectorFor(appModule).get(Logger);
        globals_1.expect(logger.transporter).toEqual([]);
    }
});
//# sourceMappingURL=module.spec.js.map