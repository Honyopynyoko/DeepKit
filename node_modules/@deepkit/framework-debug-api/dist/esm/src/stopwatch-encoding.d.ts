/// <reference types="node" />
import { FrameCategory, FrameData, FrameEnd, FrameStart } from '@deepkit/stopwatch';
import { ClassSchema } from '@deepkit/type';
export declare function encodeFrames(frames: (FrameStart | FrameEnd)[]): Uint8Array;
export declare const categorySchemas: {
    [name in FrameCategory]?: ClassSchema;
};
export declare function encodeFrameData(dataItems: FrameData[]): Buffer;
export declare function decodeFrameData(buffer: Uint8Array): {
    id: number;
    worker: number;
    bson: Uint8Array;
}[];
export declare function decodeFrames(buffer: Uint8Array): (FrameStart | FrameEnd)[];
