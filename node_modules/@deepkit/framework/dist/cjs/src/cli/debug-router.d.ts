import { Command } from '@deepkit/app';
import { Router } from '@deepkit/http';
export declare class DebugRouterController implements Command {
    protected router: Router;
    constructor(router: Router);
    execute(): Promise<void>;
}
