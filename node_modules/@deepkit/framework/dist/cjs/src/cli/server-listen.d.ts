import { Command } from '@deepkit/app';
import { InjectorContext } from '@deepkit/injector';
import { Logger } from '@deepkit/logger';
export declare class ServerListenController implements Command {
    protected logger: Logger;
    protected injectorContext: InjectorContext;
    constructor(logger: Logger, injectorContext: InjectorContext);
    execute(host?: string, port?: number, workers?: number, ssl?: boolean, selfSigned?: boolean, watch?: boolean): Promise<void>;
}
