import { FilterQuery } from '@deepkit/rpc';
export declare function findQuerySatisfied<T extends {
    [index: string]: any;
}>(target: T, query: FilterQuery<T>): boolean;
export declare function normalizeDirectory(path: string): string;
