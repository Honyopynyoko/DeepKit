/// <reference types="node" />
import { EventEmitter } from 'events';
declare class ImportedFiles extends EventEmitter {
    files: string[];
    on(event: 'file', listener: (...args: any[]) => void): this;
    add(path: string): void;
}
export declare const importedFiles: ImportedFiles;
/**
 * Watch files in importedFiles and trigger the callback when change detected
 */
export declare function watch(callback: () => void): void;
export {};
