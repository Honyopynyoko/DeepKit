"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const globals_1 = require("@jest/globals");
const rxjs_1 = require("rxjs");
globals_1.test('rxjs complete on complete', () => {
    const subject = new rxjs_1.BehaviorSubject([]);
    let completed = false;
    subject.subscribe({
        complete: () => {
            completed = true;
        }
    });
    subject.complete();
    globals_1.expect(completed).toBe(true);
});
globals_1.test('rxjs no complete on unsubscribe', () => {
    const subject = new rxjs_1.BehaviorSubject([]);
    let completed = false;
    subject.subscribe().add(() => {
        completed = true;
    });
    subject.unsubscribe();
    globals_1.expect(completed).toBe(false);
});
//# sourceMappingURL=rxjs.spec.js.map