import { Command } from '@deepkit/app';
import { ApplicationServiceContainer } from '../application-service-container';
export declare class DebugDIController implements Command {
    protected serviceContainer: ApplicationServiceContainer;
    constructor(serviceContainer: ApplicationServiceContainer);
    execute(scope?: string): Promise<void>;
}
