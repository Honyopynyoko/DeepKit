import { ClassType } from '@deepkit/core';
export declare class HttpControllers {
    readonly controllers: ClassType[];
    constructor(controllers: ClassType[]);
    add(controller: ClassType): void;
}
