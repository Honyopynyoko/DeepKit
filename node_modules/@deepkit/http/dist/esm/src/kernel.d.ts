import { InjectorContext } from '@deepkit/injector';
import { Router } from './router';
import { EventDispatcher } from '@deepkit/event';
import { Logger } from '@deepkit/logger';
import { HttpRequest, HttpResponse } from './model';
import { Stopwatch } from '@deepkit/stopwatch';
export declare class HttpKernel {
    protected router: Router;
    protected eventDispatcher: EventDispatcher;
    protected injectorContext: InjectorContext;
    protected logger: Logger;
    protected stopwatch: Stopwatch;
    constructor(router: Router, eventDispatcher: EventDispatcher, injectorContext: InjectorContext, logger: Logger, stopwatch: Stopwatch);
    handleRequestFor(method: string, url: string, jsonBody?: any): Promise<any>;
    handleRequest(req: HttpRequest, res: HttpResponse): Promise<void>;
}
