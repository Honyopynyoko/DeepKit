import { Logger } from '@deepkit/logger';
import { httpWorkflow } from './http';
export declare class HttpLogger {
    private logger;
    constructor(logger: Logger);
    onHttpRequest(event: typeof httpWorkflow.onResponse.event): void;
}
