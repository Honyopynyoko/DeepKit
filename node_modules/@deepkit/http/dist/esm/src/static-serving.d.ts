import { AppModule } from '@deepkit/app';
import { ClassType } from '@deepkit/core';
export declare function serveStaticListener(path: string, localPath?: string): ClassType;
/**
 * Serves files from a local directory at `path`. All paths without . are redirected to
 * ${localPath}/index.html.
 */
export declare function registerStaticHttpController(module: AppModule<any, any>, path: string, localPath: string): void;
