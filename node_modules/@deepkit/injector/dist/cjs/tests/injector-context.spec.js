"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
const globals_1 = require("@jest/globals");
require("reflect-metadata");
const injector_1 = require("../src/injector");
globals_1.test('context fork', () => {
    let databaseConstructed = 0;
    class Database {
        constructor() {
            databaseConstructed++;
        }
    }
    let connectionConstructed = 0;
    let Connection = class Connection {
        constructor(db) {
            connectionConstructed++;
            globals_1.expect(db).toBeInstanceOf(Database);
        }
    };
    Connection = __decorate([
        injector_1.injectable(),
        __metadata("design:paramtypes", [Database])
    ], Connection);
    const context = injector_1.InjectorContext.forProviders([
        Database,
        { provide: Connection, scope: 'http' },
    ]);
    globals_1.expect(context.get(Database)).toBeInstanceOf(Database);
    globals_1.expect(databaseConstructed).toBe(1);
    globals_1.expect(context.get(Database)).toBeInstanceOf(Database);
    globals_1.expect(databaseConstructed).toBe(1);
    {
        const httpContext = context.createChildScope('http');
        globals_1.expect(httpContext.get(Connection)).toBeInstanceOf(Connection);
        globals_1.expect(databaseConstructed).toBe(1);
        globals_1.expect(connectionConstructed).toBe(1);
        globals_1.expect(httpContext.get(Connection)).toBeInstanceOf(Connection);
        globals_1.expect(databaseConstructed).toBe(1);
        globals_1.expect(connectionConstructed).toBe(1);
    }
    {
        const httpContext = context.createChildScope('http');
        globals_1.expect(httpContext.get(Connection)).toBeInstanceOf(Connection);
        globals_1.expect(databaseConstructed).toBe(1);
        globals_1.expect(connectionConstructed).toBe(2);
        globals_1.expect(httpContext.get(Connection)).toBeInstanceOf(Connection);
        globals_1.expect(databaseConstructed).toBe(1);
        globals_1.expect(connectionConstructed).toBe(2);
    }
});
globals_1.test('injector scoped setup provider', () => {
    class MyService {
        set(value) {
            this.value = value;
        }
    }
    class MySubService extends MyService {
    }
    const context = injector_1.InjectorContext.forProviders([MyService, { provide: MySubService, scope: 'rpc' }]);
    context.configuredProviderRegistry.add(MyService, { type: 'call', methodName: 'set', args: ['foo'] });
    context.configuredProviderRegistry.add(MySubService, { type: 'call', methodName: 'set', args: ['foo'] });
    {
        const s = context.getInjector(0).get(MyService);
        globals_1.expect(s.value).toBe('foo');
    }
    const sub = context.createChildScope('rpc');
    {
        const s = sub.getInjector(0).get(MySubService);
        globals_1.expect(s.value).toBe('foo');
    }
});
//# sourceMappingURL=injector-context.spec.js.map