import { LoggerLevel, LoggerTransport } from "./logger";
export declare class MemoryLoggerTransport implements LoggerTransport {
    messages: {
        level: LoggerLevel;
        message: string;
    }[];
    messageStrings: string[];
    write(message: string, level: LoggerLevel): void;
    supportsColor(): boolean;
}
