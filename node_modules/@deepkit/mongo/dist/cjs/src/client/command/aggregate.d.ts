import { ClassType } from '@deepkit/core';
import { ClassSchema, ExtractClassType } from '@deepkit/type';
import { Command } from './command';
export declare class AggregateCommand<T extends ClassSchema | ClassType, R extends ClassSchema> extends Command {
    classSchema: T;
    pipeline: any[];
    resultSchema?: R | undefined;
    partial: boolean;
    constructor(classSchema: T, pipeline?: any[], resultSchema?: R | undefined);
    execute(config: any): Promise<ExtractClassType<R extends undefined ? T : R>[]>;
    needsWritableHost(): boolean;
}
