/// <reference types="node" />
import { MongoAuth } from './auth';
import { MongoClientConfig } from '../../config';
import { Command } from '../command';
export declare abstract class ScramAuth implements MongoAuth {
    protected mechanism: 'SCRAM-SHA-1' | 'SCRAM-SHA-256';
    protected nonce: Buffer;
    protected cryptoMethod: string;
    protected constructor(mechanism: 'SCRAM-SHA-1' | 'SCRAM-SHA-256');
    auth(command: Command, config: MongoClientConfig): Promise<void>;
    protected parseStartPayload(payload: string): {
        r: string;
        s: string;
        i: number;
    };
    protected parseContinuePayload(payload: string): {
        v: string;
    };
    protected clientFirstMessageBare(username: string, nonce: Buffer): Buffer;
}
export declare class Sha1ScramAuth extends ScramAuth {
    constructor();
}
export declare class Sha256ScramAuth extends ScramAuth {
    constructor();
}
