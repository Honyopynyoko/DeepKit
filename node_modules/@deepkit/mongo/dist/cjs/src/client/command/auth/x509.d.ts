import { MongoAuth } from './auth';
import { Command } from '../command';
import { MongoClientConfig } from '../../config';
export declare class X509Auth implements MongoAuth {
    auth(command: Command, config: MongoClientConfig): Promise<void>;
}
