import { Command } from './command';
import { ClassSchema } from '@deepkit/type';
import { ClassType } from '@deepkit/core';
export declare class DeleteCommand<T extends ClassSchema | ClassType> extends Command {
    classSchema: T;
    filter: {
        [name: string]: any;
    };
    limit: number;
    skip: number;
    constructor(classSchema: T, filter?: {
        [name: string]: any;
    }, limit?: number, skip?: number);
    execute(config: any): Promise<number>;
    needsWritableHost(): boolean;
}
