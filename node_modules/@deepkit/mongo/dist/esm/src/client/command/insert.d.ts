import { Command } from './command';
import { ClassSchema, ExtractClassType } from '@deepkit/type';
import { ClassType } from '@deepkit/core';
export declare class InsertCommand<T extends ClassSchema | ClassType> extends Command {
    protected classSchema: T;
    protected documents: ExtractClassType<T>[];
    constructor(classSchema: T, documents: ExtractClassType<T>[]);
    execute(config: any): Promise<number>;
    needsWritableHost(): boolean;
}
