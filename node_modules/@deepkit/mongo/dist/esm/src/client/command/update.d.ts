import { Command } from './command';
import { ClassSchema } from '@deepkit/type';
import { ClassType } from '@deepkit/core';
export declare class UpdateCommand<T extends ClassSchema | ClassType> extends Command {
    classSchema: T;
    updates: {
        q: any;
        u: any;
        multi: boolean;
    }[];
    constructor(classSchema: T, updates?: {
        q: any;
        u: any;
        multi: boolean;
    }[]);
    execute(config: any): Promise<number>;
    needsWritableHost(): boolean;
}
