import { DatabasePersistence, DatabasePersistenceChangeSet, DatabaseSession, Entity } from '@deepkit/orm';
import { ClassSchema } from '@deepkit/type';
import { MongoClient } from './client/client';
export declare class MongoPersistence extends DatabasePersistence {
    protected client: MongoClient;
    protected ormSequences: ClassSchema;
    protected session: DatabaseSession<any>;
    constructor(client: MongoClient, ormSequences: ClassSchema, session: DatabaseSession<any>);
    release(): void;
    remove<T extends Entity>(classSchema: ClassSchema<T>, items: T[]): Promise<void>;
    insert<T extends Entity>(classSchema: ClassSchema<T>, items: T[]): Promise<void>;
    update<T extends Entity>(classSchema: ClassSchema<T>, changeSets: DatabasePersistenceChangeSet<T>[]): Promise<void>;
}
