import { PageClass } from "./PageClass";
export declare class PageCollection {
    private readonly pages;
    constructor(pages?: PageClass[]);
    get(index: number): PageClass | null;
    count(): number;
}
