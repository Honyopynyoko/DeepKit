import { Database } from '@deepkit/orm';
import { MongoDatabaseAdapter } from '../src/adapter';
/**
 * Executes given exec() method 3 times and averages the consumed time.
 */
export declare function bench(times: number, title: string, exec: (i: number) => Promise<void> | void): Promise<void>;
export declare function createDatabase(dbName?: string): Promise<Database<MongoDatabaseAdapter>>;
