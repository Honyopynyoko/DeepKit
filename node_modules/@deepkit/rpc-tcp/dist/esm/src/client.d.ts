import { ParsedHost } from '@deepkit/core';
import { ClientTransportAdapter, TransportConnectionHooks } from '@deepkit/rpc';
/**
 * Uses `turbo-net` module to connect to the server.
 */
export declare class TcpRpcClientAdapter implements ClientTransportAdapter {
    protected host: ParsedHost;
    bufferSize: number;
    constructor(host: string);
    connect(connection: TransportConnectionHooks): Promise<void>;
}
export declare class NetTcpRpcClientAdapter implements ClientTransportAdapter {
    protected host: ParsedHost;
    constructor(host: string);
    connect(connection: TransportConnectionHooks): Promise<void>;
}
