import { Database, DatabaseRegistry } from '@deepkit/orm';
import { Migration } from './migration';
export declare class MigrationProvider {
    databases: DatabaseRegistry;
    protected databaseMap: Map<string, Database<any>>;
    protected migrationDir: string;
    constructor(databases: DatabaseRegistry);
    getMigrationDir(): string;
    setMigrationDir(dir: string): void;
    getMigrationsPerDatabase(limitDatabase?: string): Promise<Map<Database<any>, Migration[]>>;
    getMigrations(migrationDir: string): Promise<Migration[]>;
}
