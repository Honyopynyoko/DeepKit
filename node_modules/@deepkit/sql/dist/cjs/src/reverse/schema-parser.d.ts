import { Column, DatabaseModel } from '../schema/table';
import { SQLConnection } from '../sql-adapter';
import { DefaultPlatform } from '../platform/default-platform';
export declare function parseType(column: Column, type: string): void;
export declare abstract class SchemaParser {
    protected connection: SQLConnection;
    protected platform: DefaultPlatform;
    constructor(connection: SQLConnection, platform: DefaultPlatform);
    abstract parse(database: DatabaseModel, limitTableNames?: string[]): void;
}
