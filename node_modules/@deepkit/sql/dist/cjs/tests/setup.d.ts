import { Database } from '@deepkit/orm';
import { ClassSchema } from '@deepkit/type';
import { ClassType } from '@deepkit/core';
import { SQLDatabaseAdapter } from '../src/sql-adapter';
export declare function createSetup(adapter: SQLDatabaseAdapter, schemas: (ClassSchema | ClassType)[]): Promise<Database<SQLDatabaseAdapter>>;
export declare function schemaMigrationRoundTrip(types: (ClassType | ClassSchema)[], adapter: SQLDatabaseAdapter): Promise<void>;
