import { Command } from '@deepkit/app';
import { Logger } from '@deepkit/logger';
import { MigrationProvider } from '../migration/migration-provider';
import { BaseCommand } from './base-command';
export declare class MigrationCreateController extends BaseCommand implements Command {
    protected logger: Logger;
    protected provider: MigrationProvider;
    constructor(logger: Logger, provider: MigrationProvider);
    execute(database?: string, noDrop?: boolean, empty?: boolean): Promise<void>;
}
