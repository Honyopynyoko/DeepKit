import { DatabaseModel, Table, SchemaParser } from '@deepkit/sql';
export declare class SQLiteSchemaParser extends SchemaParser {
    parse(database: DatabaseModel, limitTableNames?: string[]): Promise<void>;
    protected addIndexes(database: DatabaseModel, table: Table): Promise<void>;
    protected addForeignKeys(database: DatabaseModel, table: Table): Promise<void>;
    protected addColumns(table: Table): Promise<void>;
    protected parseTables(database: DatabaseModel, limitTableNames?: string[]): Promise<void>;
}
